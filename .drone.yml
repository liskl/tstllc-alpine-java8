pipeline:
  docker:
    image: plugins/docker
    repo: registry.liskl.com/tstllc/alpine-java8
    force_tag: true

publish:
  docker:
    repo: registry.liskl.com/tstllc/alpine-java8
    tag:
      - latest
    when:
      branch:
        include: [ master, release/* ]

publish:
  docker:
    repo: registry.liskl.com/tstllc/alpine-java8
    tag:
      - ${DRONE_BRANCH}-latest
      - ${DRONE_BRANCH}.build-${DRONE_BUILD_NUMBER}
    when:
      branch:
        exclude: [ master ]
